# Config Templates

这里放的是运行配置模板，不放真实密钥，也不放机器相关的私有路径。

当前提供：

- `gemma_markdown_run_config.example.json`
  - 用于本地 LM Studio / OpenAI 兼容接口跑 Markdown 修复任务的示例配置。

使用方式建议：

1. 复制一份模板到你自己的数据工作区或私有配置目录。
2. 把其中的路径改成你的本地路径。
3. 不要把真实运行配置和密钥再次提交回 GitHub。
